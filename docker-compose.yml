version: "3"

services:
  backend:
    ports:
      - 8000:80
    build:
      context: .
    volumes:
      - ./app:/app/app
      - ./app.db:/app/app.db
    command: uvicorn app.main:app --host 0.0.0.0 --port 80 --reload
